UE.commands["kimage"] = {
    execCommand: function(cmd) {
        $('#btn-image').trigger('click');
    }
};

UE.commands["klink"] = {
    execCommand: function(cmd) {
        $('#btn-link').trigger('click');
    }
};

UE.commands["kvideo"] = {
    execCommand: function(cmd) {
        $('#btn-video').trigger('click');
    }
};